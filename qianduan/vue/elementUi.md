###Element


全局写法
```
       this.$notify({
            title: "成功",
            message: data.msg,
            type: "success"
          });
          this.$message({
            message: data.msg,
            type: "success",
            duration: 1000
          });
```
